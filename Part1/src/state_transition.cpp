#include "state_transition.hpp"
#include "op.hpp"

namespace state {

void cycle(mem_it_t begin, mem_it_t end) {
    std::size_t i = 0;
    for(auto it = begin; it != end; it++, i++) {
        //st.mem[i] = *it;
    }

}

}

